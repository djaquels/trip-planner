<%= form_with(model: ahorro, local: true) do |form| %>
  <% if ahorro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ahorro.errors.count, "error") %> prohibited this ahorro from being saved:</h2>

      <ul>
      <% ahorro.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id_viaje %>
    <%= form.number_field :id_viaje %>
  </div>

  <div class="field">
    <%= form.label :fecha_deposito %>
    <%= form.date_select :fecha_deposito %>
  </div>

  <div class="field">
    <%= form.label :numero_depostio %>
    <%= form.number_field :numero_depostio %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
